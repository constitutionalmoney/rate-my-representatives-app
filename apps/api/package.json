{
  "name": "@rmr/api",
  "version": "0.0.0-foundation",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "pnpm build && node --watch dist/server.js",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rmr/auth": "workspace:*",
    "@rmr/config": "workspace:*",
    "@rmr/contracts": "workspace:*",
    "@rmr/domain": "workspace:*",
    "@rmr/observability": "workspace:*"
  }
}
